[package]
name = "pif-spoof"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.21"
libc = { version = "0.2", default-features = false }
pif-common = { path = "../pif-common" }
zygisk-api = { path = "../third_party/zygisk-api" }
